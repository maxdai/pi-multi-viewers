---
description: 多视角协同分析——N 个视角 agent 携带主会话上下文（按预算裁剪+折叠）评审主题
argument-hint: '"<主题>" [视角分配，逗号分隔；如 "性能,可读性" 或 "3"]'
---

# Multi-Viewers（多视角协同分析）

**分析主题：$1**（视角分配：$2，缺省由你按主题建议 2-4 个互补视角）

这是用户手动触发的流程命令，不是分析内容。**严格按以下步骤执行**，
完成第 3 步后**结束当前回合**（不等待、不轮询、不转述全文）。

## 视角设计原则（写 agents/X.md 前必读）

视角之间**互补或对立都可以**——对立产生的分歧正是多视角分析的价值。
每个视角任务书必须包含：

1. **单一 lenses**：明确该 agent 用什么角度看（性能/可读性/安全/……），
   要求"所有观点必须从该视角出发"
2. **不越界**："其它视角由别的参与者负责，你不要越界展开"
3. **交锋义务**："对其它视角的观点可以认同或反驳，但要用本视角的论据"

## 步骤

### 1. 生成 spec 并填写

```bash
~/.pi/agent/npm/node_modules/pi-multi-viewers/scripts/mv.sh --prepare "<主题>" --agents <视角数>
```

然后编辑 spec 目录：

- `question.md`（第二行起）：分析对象（文件/模块/问题）、期望产出、
  是否需要达成一致
- `agents/X.md`（第二行起）：按"视角设计原则"写每个 agent 的视角任务书
- `background.md`（可选）：只写**显式约定的边界**（如"不讨论 API 设计"）。
  **不要**复述对话内容——fork 机制已让每个 agent 携带主会话上下文
  （默认 budget 模式：按预算裁剪+折叠）
- `models.md`：一般不动（默认继承本机配置）

### 2. 展示 spec 请用户审核

展示 spec 路径，**明确请用户查看/编辑**（视角任务书和主题是分析质量的
关键）——用户确认"继续"才执行第 3 步，不得跳过。

### 3. 启动分析

```bash
~/.pi/agent/npm/node_modules/pi-multi-viewers/scripts/mv.sh --start <spec目录绝对路径>
```

记录 wrapper 输出的**分析目录**，并**原样完整展示**观看命令。

### 4. 结束回合

告知用户：
- 观看：复制上一步的 `!!` 命令执行（实时流式，结束时自动退出）
- 插话：随时 wrapper `--say <目录> \"<文本>\"`（`/multi-viewers-say` 规划中）
- 完成时告诉主 pi，主 pi 会收尾

**然后结束当前回合。**

## 收尾（用户驱动）

```bash
mv.sh --status <分析目录绝对路径>   # done/stopped/running
```

- `done`：读 `<分析目录>-result.md`（与目录同级的固定位）→ 向用户给
  **摘要** → `mv.sh --cleanup <分析目录绝对路径>`
- `stopped`：报告"分析已结束但未生成结果"，不要继续等待
- `running`：告知还在进行，继续等用户通知
