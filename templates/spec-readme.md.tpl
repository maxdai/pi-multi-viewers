# 分析规格目录（--prepare 生成）

这个目录定义一次多视角分析的全部内容。编辑下面的文件，然后启动分析。

**所有文件的第一行都是说明文字，不会注入到分析环境（会被跳过）。正文从第二行开始写。**

## question.md —— 分析主题（必填）

**作用**：整个文件（跳过第一行）会变成分析环境里的 `question.md`——所有
agent 从这里了解主题与任务边界。

在这里写清**应该做什么**——任务类型决定产出形态，不要依赖上下文自明：

- **审阅** → 提出意见（哪些有问题、为什么）
- **改进建议** → 说出建议（改什么、怎么改）
- **修改** → 说明哪些可以修改、哪些需要修改（有改动权限的边界）

同时写明：分析对象（文件/模块/问题）、期望的产出（如"改进建议清单"）、
是否需要达成一致。

**为什么重要**：每个 agent 的上下文里有发起分析的完整对话历史——
其中的开发过程、未完成事项都可能被 agent 当成"该做的事"。question.md
的任务定义越明确，agent 越不会跑偏去做上下文里的其它事情。

范例（从第二行开始写）：

    # 分析主题：评审 src/calculator.py 的代码质量

    任务：审阅 src/calculator.py，从各自视角提出评审意见（本轮只
    提意见，不修改代码）。指出问题、说明理由；
    对其它视角的观点可以认同或反驳，但要用自己视角的论据。
    最终需要对"问题清单"达成一致。

## background.md —— 边界与约定（可选）

**作用**：注入每个 agent 的协议文件（AGENTS.md 背景节）。

fork 机制已让每个 agent 携带发起分析时的对话上下文（默认 budget 模式：
按预算裁剪+折叠），所以**不需要**在这里复述对话内容。这里只写**显式
约定的边界**，例如：

- 分析只聚焦某些方面（"不讨论 API 设计"）
- 已知的硬约束（"必须保持向后兼容"）
- 用户指定的优先级（"性能问题优先级最高"）

没有就留空。

## models.md —— 模型配置（可选）

每行：`agent名: model[, variant]`。model 默认 default（继承本机默认），
variant 默认 max。只有不用默认/-max 时才需要改。

## agents/X.md —— 视角任务书（每个 agent 一个）

**作用**：定义该 agent 的**视角**——它用哪副眼镜看分析对象。

写好视角任务书的要点（实测有效的措辞模式）：

1. **单一视角**：明确写出这个 agent 用什么 lenses 看（性能/可读性/
   安全/成本/用户体验/……），并要求"所有观点必须从该视角出发"
2. **不越界**：写明"其它视角由别的参与者负责，你不要越界展开"——
   实验证明这句能避免视角串味
3. **交锋义务**：写明"对其它视角的观点可以认同或反驳，但要用本视角
   的论据"——防止附和式讨论

范例（agents/a.md 正文）：

    你是多视角分析中的"性能视角"参与者（agent a）。
    你的所有观点必须从性能角度出发：复杂度、热点、不必要的计算、扩展性。
    其它视角（可读性、正确性）由别的参与者负责，你不要越界展开。

视角之间**互补或对立都可以**——对立视角产生的分歧正是多视角分析的价值。

## 下一步

编辑完成后启动分析：`scripts/mv.sh --start <本目录绝对路径>`
（或告诉主 pi "继续"，由它代为执行）。
